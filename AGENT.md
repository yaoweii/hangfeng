# AGENT 约定

## 1. 启动顺序

Codex 每次进入当前仓库后，必须先读取 `spec` 目录下的规范文件，再进行分析、设计、编码、测试或文档更新。

最低要求：

- 先读 `spec/define.md`
- 若 `spec` 目录后续新增其他规范文件，也需要一并读取

## 2. 规范优先级

在当前仓库内，数据结构、字段含义、输入输出格式统一以 `spec` 目录中的规范为准。

若出现以下冲突：

- 历史文档与 `spec` 冲突
- 旧注释与 `spec` 冲突
- 临时实现与 `spec` 冲突

则一律以 `spec` 为准。

## 3. 执行要求

在开始实现前，Codex 应先基于 `spec` 目录输出当前理解的数据定义。

要求如下：

- 不擅自增加未定义字段
- 不改变 `rxy` / `rxyz` 的既有语义
- 若发现规范缺失或冲突，先更新 `spec`，再继续实现

## 4. 文件约定

- 数据规范放在 `spec/define.md`
- `AGENT.md` 只做工作约定，不重复维护具体数据结构细节
